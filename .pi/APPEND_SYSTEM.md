## Session-start enforcement (Level 2 — pi)

Start your first reply of every session with `[ctx: AGENTS.md v2]` to confirm
context loaded.

Before responding to any user turn, you MUST read these files in order:

1. `instructions/coding-rules.md` — ⚠️ paramount behavioral contract
2. `REQUEST.md` — current task the session is working on
3. The workspace `CONTEXT.md` for the folder the task routes to
