[project]
name = "okf"
version = "0.1.0"
description = "Open Knowledge Format — conversion tool for markdown-to-OKF bundles"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["build/tests"]
pythonpath = ["build"]
